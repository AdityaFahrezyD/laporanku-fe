# Local Vite proxy target. Override in .env.local if using artisan serve.
BACKEND_URL=http://laporanku.test

# Leave empty for same-origin /api requests through the dev proxy.
# For production, use a same-origin reverse proxy or a backend origin with CORS.
VITE_API_BASE_URL=
